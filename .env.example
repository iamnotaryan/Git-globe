# Optional GitHub token for local development.
#
# WARNING: because this uses the VITE_ prefix, Vite inlines it into the
# client-side JavaScript bundle at build time. Anyone who opens the app can
# extract the token from the browser. Use a minimal-scope, fine-grained token
# and NEVER deploy this publicly. See README.md.
VITE_GITHUB_TOKEN=